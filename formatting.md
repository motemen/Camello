# Perl Formatter Specification

このドキュメントは、Camello フォーマッタの動作仕様を定義します。

## 1. 基本方針

### 1.1 不変の原則

- **トリビア以外は変更しない**: フォーマッタはトークン（空白、改行、インデント以外）の内容や順序を変更しません
- **構造の保存**: コードの構造（AST）は変更されません
- **意味の保存**: フォーマット後のコードは、フォーマット前と完全に同じ意味を持ちます

### 1.2 ユーザ意図の尊重

- **改行の尊重**: ユーザが入力した改行は場所を問わず基本的に保持されます。フォーマッタは自動的な改行の挿入や削除を最小限に留めます
- **構造化フォーマット**: 一部の構文要素（制御構造、サブルーチン定義など）では、K&Rスタイルのブロックフォーマットなど一貫した形式に正規化されます
- **空行の自動挿入なし**: サブルーチン定義やフェーズブロックの前後を除き、空行を自動的に挿入しません。ユーザーが入力した空行を保持するのみです

## 2. 改行のルール

### 2.1 ユーザ改行の保持

原則として、ユーザが入力した改行は保持されます。

```perl
# ユーザが入力した改行を保持
my $x = 1
    + 2;

my $result = $test
    ? "true_value"
    : "false_value";
```

### 2.2 空行の正規化

連続する空行（2行以上の空行）は1行にまとめられます。

```perl
# 入力（3行の空行）
my $x = 1;



my $y = 2;

# 出力（1行の空行）
my $x = 1;

my $y = 2;
```

### 2.3 文の終端

セミコロン（`;`）の後には改行が挿入されます。

```perl
# 入力
my $x=1;my $y=2;

# 出力
my $x = 1;
my $y = 2;
```

ただし、セミコロンの後にインラインコメントがある場合は、コメントの後に改行します。

```perl
my $x = 1;  # コメント
my $y = 2;
```

### 2.4 制御構造

#### 2.4.1 ブロック形式

開きブレース（`{`）の後と閉じブレース（`}`）の前後には改行が挿入されます。

```perl
# K&Rスタイル
if ($condition) {
    statement;
}

while ($x < 10) {
    process();
}

sub foo {
    my $x = 1;
}
```

#### 2.4.2 単文ブロック（simple block）

以下の条件をすべて満たすブロックは「単文ブロック」として1行に収められます：

- セミコロンが含まれない
- 文が1つ以下
- コメントが含まれない
- フェーズブロック（BEGIN, END など）ではない
- 空行がない

**重要な例外**：**制御構造（`if`, `unless`, `while`, `until`, `for`, `foreach`, `given`）のブロックは、単文ブロックの条件を満たしていても常に複数行にフォーマットされます。**

```perl
# 制御構造は単文ブロックでも常に複数行
if ($x) {
}

while ($i < 10) {
}

for (; ; ) {
}

# 制御構造以外は単文ブロックなら1行
try { risky() } catch { fallback() }
my $code = sub { };
my $value = do { $x + $y };
map { $_ * 2 } @list
grep { /pattern/ } @data
```

複数行にフォーマットされる例：

```perl
# セミコロンあり
for my $var (@list) {
    my $x = 1;
    print $x;
}

# コメントあり
map {
    # comment
    $_ * 2;
} @list
```

#### 2.4.3 チェーンされたキーワード

`elsif`, `else`, `catch`, `finally`, `when`, `default` は前の閉じブレースと同じ行に配置されます。

```perl
if ($x) {
    foo();
} elsif ($y) {
    bar();
} else {
    baz();
}

try {
    risky();
} catch ($err) {
    handle($err);
} finally {
    cleanup();
}
```

#### 2.4.4 後置条件

後置条件（postfix conditionals）は1行に収められます。

```perl
warn 1 if $err;
print $_ for @values;
say $x while $x > 0;
```

ユーザが改行を入れた場合は、継続インデントが適用されます。

```perl
warn 1
    if $err;
```

### 2.5 リテラルの複数行展開

#### 2.5.1 配列リファレンス

開き括弧の直後（トリビアを除く）に改行がある場合、すべての要素を複数行に展開します。

```perl
# 入力（最初の要素の前に改行あり）
my @items = (
$first, $second, $third
);

# 出力（全要素を複数行に展開）
my @items = (
    $first,
    $second,
    $third
);
```

開き括弧と最初の要素が同じ行にある場合は、ユーザの改行を保持します。

```perl
# ユーザの改行を保持
my %hash = (key1 => 'value1',
    key2 => 'value2');
```

#### 2.5.2 ハッシュリファレンス

配列リファレンスと同じルールが適用されます。

```perl
# 複数行に展開
my %config = (
    key1 => "value1",
    key2 => "value2"
);

# ユーザの改行を保持
my %hash = (key1 => 'value1',
    key2 => 'value2');
```

#### 2.5.3 qw 式

`qw` 式も同様に、開き括弧の直後に改行がある場合は複数行に展開されます。

### 2.6 括弧式の複数行フォーマット

開き括弧の直後に改行がある場合、閉じ括弧まで複数行フォーマットが適用されます。

```perl
# 複数行フォーマット
if (
    $condition1
        && $condition2
) {
    warn "matched";
}

my $output = some_function(
    $arg1,
    $arg2
);
```

### 2.7 ブロック関数呼び出し

`grep`, `map`, `sort` などのブロック関数のブロックには、通常のブロックと同じフォーマット規則が適用されます。

**セミコロンが含まれる場合**：複数行にフォーマットされます。

```perl
# セミコロンがあるため複数行
my @filtered = grep {
    $_->is_valid;
} @items;

my @transformed = map {
    $_ * 2;
} @data;
```

**単文ブロックの条件を満たす場合**（セミコロンなし、文1つ以下、空行なし）：1行に収められます。

```perl
# セミコロンがないため1行
my @doubled = map { $_ * 2 } @numbers;
my $result = try { risky() } catch { fallback() };
```

## 3. インデントのルール

### 3.1 基本インデント

- インデントは4スペース
- ブロック内では1段階（4スペース）インデント
- ネストしたブロックは累積的にインデント

```perl
sub foo {
    if ($x) {
        while ($y) {
            process();
        }
    }
}
```

### 3.2 継続インデント（Continuation Indent）

行が続く場合（式が複数行にわたる場合）、2行目以降に追加で4スペースのインデントが適用されます。

#### 3.2.1 演算子の後

```perl
my $sum = $value1
    + $value2
    + $value3
    * $multiplier;

my $allowed = $user->has_permission()
    && $feature->enabled()
    || $is_admin;
```

#### 3.2.2 カンマの後（式の途中で改行した場合）

式の途中でカンマの後に改行がある場合、継続インデントが適用されます。

```perl
my $result = func($a,
    $b);
```

**注意**：開き括弧の直後に改行がある場合は、継続インデントではなく括弧のインデント（セクション3.3参照）が適用されます。

#### 3.2.3 後置キーワード

```perl
warn 1
    if $err;
```

#### 3.2.4 三項演算子

```perl
my $result = $test
    ? "true_value"
    : "false_value";
```

#### 3.2.5 package 文

```perl
package
    My::Module::Name;
```

#### 3.2.6 メソッドチェーン（ユーザーが改行した場合）

ユーザーがメソッドチェーンの途中で改行した場合、継続インデントが適用されます。

```perl
my $obj = $factory
    ->create()
    ->initialize()
    ->configure();
```

フォーマッタは自動的にメソッドチェーンを改行しません。ユーザーの改行を保持するのみです。

### 3.3 括弧とブレースのインデント

開き括弧またはブレースの**直後に改行がある場合**、その中身は1段階（4スペース）インデントされます。これは継続インデントとは異なるルールです。

```perl
# 開き括弧の直後に改行 → 括弧のインデント
my $output = some_function(
    $arg1,
    $arg2
);

# ハッシュリファレンス
my %config = (
    key1 => "value1",
    key2 => "value2"
);

# if文の条件
if (
    $condition1
        && $condition2
) {
    statement;
}
```

**継続インデントとの違い**：

- **括弧のインデント**：開き括弧の直後に改行 → 中身を1段階インデント
- **継続インデント**：式の途中で改行 → 次の行に継続インデント

```perl
# 括弧のインデント
func(
    $arg1,
    $arg2
)

# 継続インデント
func($arg1,
    $arg2)
```

### 3.4 サブルーチン内での継続インデント

サブルーチン内で継続インデントが発生する場合、ブロックのインデント（4スペース）に継続インデント（4スペース）が加わります。

```perl
sub foo {
    my $result = func($a,
        $b);
}
```

## 4. 空白（Inline Spacing）のルール

### 4.1 演算子周りの空白

#### 4.1.1 二項演算子

二項演算子の前後にスペースを入れます。

```perl
my $x = 1 + 2 * 3;
my $s = "hello" . " " . "world";
$a == $b
$a eq $b
$a && $b
$a || $b
$a // $b
$a cmp $b
$a <=> $b
```

#### 4.1.2 代入演算子

代入演算子（`=` およびその複合形）の前後にスペースを入れます。

```perl
my $x  = 1;
$x    += 2;
$str  .= "suffix";
$flag ||= default_value();
```

複合代入演算子では、演算子と `=` の間にスペースを入れません。

```perl
$x += 1;   # 正しい
$x + = 1;  # 誤り（フォーマッタはこうしない）
```

#### 4.1.3 比較演算子

比較演算子の前後にスペースを入れます。

```perl
$a < $b
$a > $b
$a <= $b
$a >= $b
$a != $b
$a ne $b
```

#### 4.1.4 アロー演算子

アロー演算子（`->`）の前後にはスペースを入れません。

```perl
$obj->method()
$ref->{key}
$ref->[0]
```

#### 4.1.5 範囲演算子

範囲演算子（`..`, `...`）の前後にスペースを入れます。

```perl
1 .. 10
0 ... $#array
```

#### 4.1.6 繰り返し演算子

繰り返し演算子（`x`）の前後にスペースを入れます。

```perl
"a" x 10
q(a) x 10
```

#### 4.1.7 三項演算子

三項演算子（`? :`）では、`?` と `:` の前後にスペースを入れます。

```perl
my $result = $test ? "yes" : "no";
```

**実装上の注意**：スペースは `?` や `:` トークン自体の後ではなく、後続するトークンの前に挿入されます。これは他の演算子と同じスペーシングの仕組みです。

### 4.2 前置・後置演算子

#### 4.2.1 前置演算子

前置演算子とオペランドの間にはスペースを入れません。

```perl
!$flag
~$bits
++$i
--$j
-$x
+$y
```

#### 4.2.2 後置演算子

後置演算子とオペランドの間にはスペースを入れません。

```perl
$i++
$j--
$x->@*
$y->%*
```

#### 4.2.3 ファイルテスト演算子

ファイルテスト演算子の後にはスペースを入れます。

```perl
-f $file
-d $dir
-e $path
```

### 4.3 カンマ

カンマ（`,`）の前にはスペースを入れず、後にはスペースを入れます。

```perl
my @list = (1, 2, 3);
func($a, $b, $c);
```

ただし、末尾のカンマの後にはスペースを入れません（閉じ括弧が続くため）。

```perl
my @list = (1, 2, 3, );
```

### 4.4 ファットカンマ

ファットカンマ（`=>`）は二項演算子として扱われ、前後にスペースを入れます。

```perl
my %hash = (
    key1 => "value1",
    key2 => "value2"
);
```

### 4.5 括弧とブレース

#### 4.5.1 丸括弧

**デフォルト設定（Tight）**：内側にスペースを入れません。

```perl
func($a, $b)
($x + $y) * $z
```

ただし、制御構造の条件式では、開き括弧の前にスペースを入れます。

```perl
if ($condition) { ... }
while ($x < 10) { ... }
for my $i (0 .. 9) { ... }
```

関数呼び出しでは、開き括弧の前にスペースを入れません。

```perl
func($arg)
my_function($x, $y)
```

**設定変更可能**：`DelimiterTightness` 設定で変更可能です。

#### 4.5.2 角括弧

**デフォルト設定（Standard）**：内側に要素が2つ以上ある場合はスペースを入れます。

```perl
[1, 2, 3]    # スペースあり
[$x]         # スペースなし
[]           # スペースなし
```

**設定変更可能**：`DelimiterTightness` 設定で変更可能です。

#### 4.5.3 波括弧

波括弧のスペースは、用途によって異なるルールが適用されます。

**ハッシュリファレンス/リテラル**：

デフォルト設定（Standard）では、内側に要素が2つ以上ある場合はスペースを入れます。

```perl
{ a => 1, b => 2 }    # 要素2つ以上 → スペースあり
{ $single }           # 要素1つ → スペースなし
{}                    # 空 → スペースなし
```

**単文ブロック**：

単文ブロック（セクション2.4.2参照）では、内側にスペースを1つ入れます。

```perl
# 単文ブロック
map { $_ * 2 } @list         # スペースあり
grep { /pattern/ } @data     # スペースあり
do { $x + $y }               # スペースあり
sub { }                      # 空でもスペースあり
```

**制御構造のブロック**：

制御構造（`if`, `unless`, `while`, `until`, `for`, `foreach`, `given`）のブロックは、単文ブロックの条件を満たしていても**必ず複数行にフォーマットされます**（セクション2.4.2参照）。空のブロックも例外ではありません。

```perl
# 空のブロックも複数行
if ($x) {
}

while ($condition) {
}

for (; ; ) {
}

# セミコロンがある場合も複数行
if ($x) {
    ;
}

# 内容がある場合も複数行（改行されるためスペースは関係なし）
if ($x) {
    statement;
}
```

**注意**：現在の実装では、空の制御構造ブロックが `if ($x) { }` のように1行になっていますが、これは仕様に合っていません。実装を修正する必要があります。

**設定変更可能**：`DelimiterTightness` 設定で変更可能です。

#### 4.5.4 開き括弧の前

- 制御構造キーワード（`if`, `while`, `for` など）の後：スペースあり
- 変数の後（デリファレンスや添字）：スペースなし
- 関数呼び出し：スペースなし
- キーワードではない識別子（関数名）：スペースなし

```perl
# スペースあり
if ($x) {
}
while ($y) {
}
for my $i (@list) {
}

# スペースなし
$obj->method($arg)
$hash{key}
$array[0]
func($x, $y)
$var{key}
```

### 4.6 キーワード

#### 4.6.1 宣言キーワード

宣言キーワード（`my`, `our`, `state`, `local`）の後にはスペースを入れます。

```perl
my $x = 1;
our @array = ();
state $counter = 0;
local $/ = undef;
```

#### 4.6.2 制御構造キーワード

制御構造キーワードの後にはスペースを入れます。

```perl
if ($x) { }
unless ($y) { }
while ($z) { }
for my $i (@list) { }
```

#### 4.6.3 return および制御キーワード

`return`, `next`, `last`, `redo` の後は、セミコロンの前を除いてスペースを入れます。

```perl
return $value;
return;        # セミコロンの前はスペースなし
next if $skip;
last LABEL;
```

#### 4.6.4 package, use, require

これらのキーワードの後にはスペースを入れます。

```perl
package Foo::Bar;
use strict;
require Module;
```

### 4.7 シジル

シジル（`$`, `@`, `%`, `&`, `*`）とその後の変数名の間にはスペースを入れません。

```perl
$scalar
@array
%hash
&code
*glob
```

### 4.8 ダブルコロン

ダブルコロン（`::`）の前後にはスペースを入れません。

```perl
Foo::Bar::baz()
my $x = $Foo::Bar::CONSTANT;
```

### 4.9 セミコロン

セミコロンの前にはスペースを入れず、後にはスペースを入れます（ただし、通常は改行されます）。

```perl
my $x = 1; my $y = 2;  # 同じ行の場合
```

### 4.10 変数の後

変数の後に別の変数、識別子、文字列が続く場合はスペースを入れます（ファイルハンドル構文のため）。

```perl
print $fh $output;
print STDERR "error\n";
```

ただし、デリファレンスやアクセス演算子が続く場合はスペースを入れません。

```perl
$var->method()
$var->{key}
$var->[0]
$var->@*
```

## 5. 空行のルール

### 5.1 サブルーチン定義

サブルーチン定義の前後には空行を入れます。

```perl
use strict;

sub foo {
    ...
}

sub bar {
    ...
}
```

ただし、ファイルの先頭やブロックの先頭では前の空行を入れません。

```perl
# ファイルの先頭
sub first {
    ...
}

# サブルーチン内のBEGINブロック
sub foo {
    BEGIN {
        ...
    }
}
```

### 5.2 フェーズブロック

フェーズブロック（`BEGIN`, `END`, `CHECK`, `INIT`, `UNITCHECK`）の前後には空行を入れます。

```perl
use strict;

BEGIN {
    ...
}

CHECK {
    ...
}

END {
    ...
}
```

### 5.3 use/no 文

`use` や `no` 文は、`package` 文の直後を除いて、前後に空行を入れます。

```perl
package Foo;
use strict;    # package の直後は空行なし
use warnings;

sub bar {      # use の後は空行あり
    ...
}
```

### 5.4 通常の文

通常の文（`my` 宣言、式文など）では、ソースコードに空行がある場合のみそれを保持します。自動的に空行を挿入することはありません。

```perl
# ユーザが空行を入れた場合のみ保持
my $x = 1;

my $y = 2;  # ユーザが空行を入れた

my $z = 3;  # ユーザが空行を入れなかった
```

### 5.5 PODブロックと__DATA__セクションの前

PODブロック（`=head1`, `=pod` など）と `__DATA__` セクションの前には、空行を1行入れます。

```perl
my $code = 1;

=head1 NAME

Module - description

=cut

sub foo {
    ...
}

my $last_code = 1;

__DATA__
data content
```

### 5.6 ファイル終端

ファイルの終端には空行を入れません。最後の文の後は改行のみで終了します。

```perl
sub last {
    my $x = 1;
}
# ↑ ここで終了（改行のみ、空行なし）
```

## 6. コメント

### 6.1 行末コメント

行末コメントは、前のトークンとの間にスペース1つを入れて配置します。

```perl
my $x = 1;  # コメント
```

### 6.2 独立行コメント

独立行のコメントは、現在のインデントレベルでインデントされます。

```perl
sub foo {
    # このコメントはサブルーチン内のインデント
    my $x = 1;
}
```

### 6.3 コメントの保持

すべてのコメントは、その内容と相対的な位置を保持します。

```perl
# 入力
my $x=1;# comment

# 出力
my $x = 1;  # comment
```

### 6.4 演算子の後のコメント

演算子の後にコメントがある場合、コメントの後に継続インデントが適用されます。

```perl
my $x = 1
# adjust
    + 2;
```

## 7. アライメント（整列）

### 7.1 代入演算子のアライメント

連続する代入文では、代入演算子（`=` およびその複合形）を揃えます。

```perl
my $x     = 1;
our $y    = 2;
state $z  = 3;
local $w  = 4;
```

複合代入演算子も含めてアライメントされます（デフォルト設定）。

```perl
$x   = 1;
$y  += 2;
$z  .= "str";
```

**設定変更可能**：`align_compound_assignments` オプションで無効化可能です。

### 7.2 ファットカンマのアライメント

ハッシュリテラルやリストでは、ファットカンマ（`=>`）を揃えます。

```perl
my %hash = (
    key1   => "value1",
    key2   => "value2",
    longer => "value3"
);
```

### 7.3 アライメントのスコープ

アライメントは、連続する行の範囲で適用されます。**アライメント対象のトークン（`=`、`=>`など）が登場しない行が現れた時点で、アライメントグループは直ちに終了します。**

```perl
# アライメントグループ1
my $x = 1;
my $y = 2;

# 空行でグループ終了

# アライメントグループ2
my $a = 3;
my $b = 4;

# 異なる種類の文でグループ終了
if ($condition) {
    # この中の代入は別グループ
    my $inner = 5;
}

# アライメントグループ3
my $c = 6;
my $d = 7;
```

具体例：

```perl
# ケース1: 空行で区切られる
my $foo    = 1;
my $bar    = 2;

my $baz    = 3;  # 新しいグループ
my $qux    = 4;

# ケース2: 代入のない行で区切られる
my $x      = 1;
my $y      = 2;
say "hello";      # 代入なし → グループ終了
my $z      = 3;   # 新しいグループ

# ケース3: 制御構造で区切られる
my $a      = 1;
if ($condition) {
    my $b = 2;    # 別グループ（インデントレベルも異なる）
}
my $c      = 3;   # 新しいグループ
```

**設定変更可能**：`alignment_strategies` オプションでアライメント機能を選択的に有効/無効化できます。

## 8. 特殊な構文

### 8.1 ヒアドキュメント

ヒアドキュメントの内容は一切変更されません。

```perl
my $text = <<'EOF';
    このインデントも保持される
    改行も保持される
EOF
```

### 8.2 POD

POD ブロックは一切変更されません。

```perl
=head1 TITLE

このPODは変更されない

=cut
```

### 8.3 __DATA__ セクション

`__DATA__` セクションの内容は一切変更されません。

```perl
__DATA__
このデータは変更されない
    インデントも保持される
```

### 8.4 Quote-like 演算子

Quote-like 演算子（`q`, `qq`, `qw`, `qx`, `m`, `s`, `tr`, `y`, `qr`）の内容は変更されません（デリミタの外側は通常通りフォーマットされます）。

```perl
my $str = q{raw string};
my @words = qw(one two three);
s{pattern}{replacement};
```

### 8.5 ラベル

ラベルは独立行に配置され、後のコロンはそのまま続きます。

```perl
LABEL: while ($condition) {
    next LABEL if $skip;
}
```

ラベルとコロンの間の空白は削除されます。

```perl
# 入力
LABEL : while ($x) { }

# 出力
LABEL: while ($x) { }
```

## 9. 追加の仕様詳細

このセクションでは、セクション1-8で説明しきれなかった詳細な仕様を補足します。

### 9.1 単文ブロックの完全な定義

単文ブロック（simple block）の定義（セクション2.4.2で説明済み）を明確にします：

**空のブロックや式のみのブロックも単文ブロックです：**

```perl
# すべて単文ブロック（セミコロンなし、コメントなし、空行なし）
my $code = sub { };         # 空のブロック
my $value = do { $x + $y }; # 式のみ（文ではない）
my $ref = \&{ $name };      # 式のみ
```

これらは定義から明白に単文ブロックの条件を満たしています。

### 9.2 括弧内のスペース（確定）

**デフォルト設定（Tight）を使用します：**

丸括弧、角括弧、波括弧すべてについて、`DelimiterTightness` のデフォルト設定を使用します：

- **丸括弧**: Tight（内側にスペースを入れない）
- **角括弧**: Standard（要素が2つ以上ある場合スペースあり）
- **波括弧**: Standard（要素が2つ以上ある場合スペースあり）

```perl
# Tight設定による出力
func($a, $b)
my $result = ($x + $y) * $z;
if (($a || $b) && $c) { }
```

コンテキスト依存のスペーシング（関数呼び出し vs 数式）は行いません。ユーザー設定で統一します。

### 9.3 コメントのアライメント（確定）

**コメントもアライメントし、オフセット制限は設けません：**

行末コメントは、代入演算子やファットカンマと同様にアライメントされます（`AlignmentStrategy::Comments` がデフォルトで有効）。

```perl
# コメントがアライメントされる
my $x = 1;                           # short comment
my $very_long_variable_name = 2;     # another comment
my $z = 3;                           # yet another

# すべての行にコメントがある場合は揃う
my $a = 1;  # first comment
my $b = 2;  # second comment
my $c = 3;  # third comment

# コメントがない行が現れるとグループ終了
my $x = 1;  # comment
my $y = 2;  # comment
my $z = 3;
# ↑ $z にはコメントがない → ここでグループ終了
my $w = 4;  # comment (新しいグループ、xやyとは揃わない)

# TODOなど種類に関わらず揃える
my $x = 1;     # TODO: fix this
my $y = 2;     # OK
```

コメント位置の最大オフセット制限は設けず、グループ内で自然にアライメントされます。

### 9.4 連続する空行の扱い（確定）

**基本ルール**：連続する空行（2行以上）は1行にまとめられます。

**例外**：以下のセクションの前には空行を1行確保します（セクション5.5で説明済み）：

- PODブロック（`=head1`, `=pod` など）の前
- `__DATA__` セクションの前

**ファイル終端**：ファイルの終端には空行を入れません（セクション5.6で説明済み）。

```perl
# 正しいフォーマット
my $code = 1;

=head1 NAME

Description

=cut

sub foo {
    # ...
}

my $last = 1;

__DATA__
data
# ↑ ファイル終端（改行のみ、空行なし）
```

## 10. 今後の拡張

### 10.1 設定可能なオプション

以下のオプションを設定可能にすることを検討：

- インデント幅（現在は4スペース固定）
- タブ vs スペース
- 行の最大長（現在は制限なし）
- アライメントの詳細設定

### 10.2 スタイルプリセット

異なるコーディングスタイル（例: PBP, Perl::Tidy 互換など）のプリセットを提供することを検討。

### 10.3 エディタ統合

エディタでの保存時自動フォーマットや、部分的なフォーマット（選択範囲のみ）をサポートすることを検討。

---

このドキュメントは、実装とテストケースに基づいて作成されましたが、フォーマッタの仕様は進化する可能性があります。不明確な点や矛盾する動作を発見した場合は、GitHub Issues で報告してください。
