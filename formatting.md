# Perl Formatter Specification

このドキュメントは、Camello フォーマッタの動作仕様を定義します。

## 1. 基本方針 (POLICY)

### 1.1 不変の原則

- (POLICY-1) **意味の保存**: フォーマット後のコードは、フォーマット前と完全に同じ意味を持ちます。
- (POLICY-2) **構造の保存**: コードのAST（抽象構文木）は変更されません。
- (POLICY-3) **トークンの保存**: 空白、改行、インデントなどのトリビアを除き、トークンの内容や順序は変更されません。

### 1.2 ユーザー意図の尊重

- (POLICY-4) **改行の保持**: ユーザーが入力した改行は、式の途中であっても基本的に保持されます。フォーマッタによる自動的な改行の挿入や削除は最小限に留められます。
- (POLICY-5) **空行の保持**: ユーザーが入力した空行は、連続する場合に1行に正規化された上で保持されます。フォーマッタが自動で空行を挿入するのは、`sub`定義や`package`文の周辺など、一部の構造的な箇所に限定されます。

## 2. インデント (INDENT)

(INDENT-1) インデントは**4スペース**を基本とします。

### 2.1 括弧とブロックのインデント (INDENT-2)

開き括弧 (`(`, `[`, `{`) の**直後に改行がある場合**、その括弧が閉じるまでの中身全体が1段階（4スペース）インデントされます。

`{}` ブロックの場合、インデントはネストで累積します。また、配列/ハッシュリテラルや`qw`式では、各要素が1行ずつに展開されます。

```perl
# コードブロックのインデント（ネストで累積）
sub foo {
    if ($x) {
        while ($y) {
            process(); # 3段階のインデント
        }
    }
}

# 関数呼び出しの括弧内インデント
func(
    $arg1,
    $arg2
);

# 配列リテラルの展開
my @items = (
    $first,
    $second,
);

# 開き括弧の直後に改行がない場合は、ユーザーの改行を尊重
my %hash = (key1 => 'value1',
    key2 => 'value2');
```

このルールは、関数呼び出し、リスト、ハッシュリファレンス、`if`文の条件式など、括弧で囲まれたあらゆる構造に適用されます。

### 2.2 継続インデント (INDENT-3)

複数行にわたる式では、2行目以降に**継続インデント**（追加の4スペース）が適用されます。これは、演算子、カンマ、後置キーワードの後などで改行した場合に発生します。

```perl
# 演算子の後で改行
my $sum = $value1
    + $value2;

# カンマの後で改行
my $result = func($a,
    $b);

# 後置キーワードの前で改行
warn "error"
    if $err;
```

## 3. 改行 (NEWLINE)

### 3.1 文の終端 (NEWLINE-1)

各文の終端（通常はセミコロン `;` の後）には改行が挿入されます。

```perl
# 入力
my $x=1;my $y=2;

# 出力
my $x = 1;
my $y = 2;
```

### 3.2 ブロックのフォーマット (NEWLINE-2)

ブロック (`{...}`) のフォーマットは、そのコンテキストによって決まります。

#### 単文ブロックと複数行ブロック

以下の条件をすべて満たすブロックは**単文ブロック**として扱われ、1行にフォーマットされる可能性があります。

- 中に文が1つ以下
- セミコロン、コメント、空行を含まない

上記を満たさない場合は**複数行ブロック**となり、K&Rスタイル（開きブレースがキーワードと同じ行にあり、中身がインデントされる）でフォーマットされます。

#### コンテキストごとのルール

- **常に複数行になるブロック**:
  制御構造（`if`, `unless`, `while`, `for`, `given`など）のブロックは、単文ブロックの条件を満たす場合でも、**常に複数行**でフォーマットされます。
  ```perl
  if ($condition) {
      statement;
  }
  # 空ブロックも複数行
  while ($x) {
  }
  ```

- **条件に応じて1行になるブロック**:
  `map`, `grep`, `sub {}`, `do {}`, `try {}` などのブロックは、単文ブロックの条件を満たせば1行に、そうでなければ複数行になります。
  ```perl
  # 1行になる例
  map { $_ * 2 } @list;
  try { risky() } catch { fallback() };

  # 複数行になる例 (セミコロンがあるため)
  map {
      $_ * 2;
  } @list;
  ```

### 3.3 チェーンされたキーワード (NEWLINE-3)

`elsif`, `else`, `catch`, `finally`, `when`, `default` は、前の閉じブレース `}` と同じ行に配置されます。

```perl
if ($x) {
    ...
} elsif ($y) {
    ...
} else {
    ...
}
```

## 4. 空白 (Inline Spacing) (SPACING)

### 4.1 演算子

- (SPACING-1) **前後にスペースを入れる演算子**:
  - 二項演算子: `+`, `*`, `.`, `&&`, `eq` など
  - 代入演算子: `=`, `+=`, `||=` など
  - 比較演算子: `==`, `!=`, `>`, `lt` など
  - ファットカンマ: `=>`
  - その他: `..`, `...`, `x`, `?`, `:`
  ```perl
  my $x = 1 + 2;
  $x += 3;
  my $h = { key => 'val' };
  ```

- (SPACING-2) **前後にスペースを入れない演算子**:
  - アロー演算子: `->`
  - ダブルコロン: `::`
  ```perl
  $obj->method();
  My::Class->new();
  ```

- (SPACING-3) **オペランドとの間にスペースを入れない演算子**:
  - 前置演算子: `!`, `-`, `+`, `++`, `--`
  - 後置演算子: `++`, `--`
  ```perl
  !$flag;
  ++$i;
  $j--;
  ```

- (SPACING-4) **特殊なケース**:
  - ファイルテスト演算子: 後にスペースを入れる (`-f $file`)

### 4.2 区切り文字

- (SPACING-5) **カンマ (`,`)**: 前にスペースを入れず、後にスペースを入れる。
- (SPACING-6) **セミコロン (`;`)**: 前にスペースを入れず、後には（改行されない場合）スペースを入れる。

### 4.3 括弧・ブレース

(SPACING-7) 角括弧・波括弧リテラルの内側のスペースは `delimiter_spacing` 設定（`tight` / `standard` / `loose`、既定は `standard`）に依存しますが、デフォルトの挙動は以下の通りです。

- **丸括弧 `()`**: 内側にスペースを入れない (Tight)。
  ```perl
  func($a, $b);
  ($x + $y) * $z;
  ```
- **角括弧 `[]` / 波括弧 `{}`**: 内側にスペースを入れる (Standard)。ただし、
  中身が**単純項ひとつだけ**——変数・リテラル・名前、およびそれらへの前置演算子
  (`-1`, `\$x`)——の場合は入れません。
  ```perl
  [ 1, 2, 3 ];
  [$x];               # 単純項ひとつ
  [-1];               # 符号付きの数もひとつ
  { a => 1, b => 2 };
  {$single};          # 単純項ひとつ
  [ foo($body) ];     # 呼び出しは単純項ではない
  { $obj->qux };
  [ map { $_->foo } @$list ];
  ```
- **デリファレンスの波括弧 `@{ ... }`**: 同じ規則です。名前や変数ひとつなら密着し、
  構造を持つ式なら内側にスペースを入れます。`${^MATCH}` のように波括弧の中身が
  一つの名前であるものは、スペースを入れると**別の変数になる**ため必ず密着します。
  ```perl
  @{$ref};
  @{ $ref->{bar} };
  ${^MATCH};
  ```
- **単文ブロック `{}`**: 内側にスペースを1つ入れる。
  ```perl
  map { $_ * 2 } @list;
  sub { }; # 空でもスペースあり
  ```

### 4.4 キーワードと括弧

- (SPACING-8) **スペースを入れる**: 制御キーワード (`if`, `while`, `for` など) の後。
  ```perl
  if ($condition) { ... }
  ```
- (SPACING-9) **スペースを入れない**: 関数呼び出し、デリファレンス、添字アクセス。
  ```perl
  func($arg);
  $hash{key};
  $array[0];
  ```

### 4.5 その他

- (SPACING-10) **シジル (`$`, `@`, `%` 等)**: 変数名との間にスペースを入れない (`$scalar`)。
- (SPACING-11) **ラベル**: コロンとの間にスペースを入れない (`LABEL:`)。
- (SPACING-12) **ファイルハンドル**: `print`文などでファイルハンドルと引数を区切るためにスペースを入れる (`print STDERR "error"`).

## 5. 空行 (BLANK_LINE)

### 5.1 自動的に挿入される空行 (BLANK_LINE-1)

- **`sub`定義**: 前後に1行ずつ。
- **フェーズブロック (`BEGIN`, `END`等)**: 前後に1行ずつ。
- **`package`, `use`, `no`文**: 文のグループの前後（ただし`package`直後の`use`など、関連する宣言が続く場合は除く）。
- **PODブロック, `__DATA__`セクション**: 直前に1行。

```perl
package My::App;
use strict;
use warnings;

my $VERSION = 1;

sub new {
    ...
}

sub old {
    ...
}

1;

__END__
data
```

### 5.2 ユーザー入力の保持と正規化

- (BLANK_LINE-2) 文と文の間にある空行は保持されます。
- (BLANK_LINE-3) 連続する2行以上の空行は、1行に正規化されます。
- (BLANK_LINE-4) ファイルの終端には余分な空行は挿入されません。

## 6. コメント (COMMENT)

- (COMMENT-1) **コメントの保持**: すべてのコメントは、その内容と相対的な位置を保持します。
- (COMMENT-2) **インデント**: 独立行のコメントは、その場所のインデントレベルに従います。
- (COMMENT-3) **行末コメント**: トークンの後に最小4スペース（`min_spaces_before_comment` 設定、既定4）を挟んで配置されます。アライメントの対象にもなります（セクション7参照。整列される場合は整列が優先されます）。
  ```perl
  my $x = 1;    # コメントはインデントされる
  ```
- (COMMENT-4) **継続インデントとコメント**: 演算子の後などにコメントがある場合、コメントの次の行から継続インデントが適用されます。
  ```perl
  my $x = 1 # comment
      + 2;
  ```

## 7. アライメント（整列） (ALIGNMENT)

### 7.1 アライメントのスコープ (ALIGNMENT-1)

アライメントは、**連続する行**で構成されるグループ内でのみ適用されます。以下のいずれかの条件でアライメントグループは終了します。

- **空行**が現れた場合
- **アライメント対象トークンを含まない行**が現れた場合
- **異なる種類の文**（制御構造など）が現れた場合

### 7.2 アライメント対象 (ALIGNMENT-2)

- **代入演算子 (`=`, `+=` 等)**
- **ファットカンマ (`=>`)**
- **行末コメント (`# ...`)**

```perl
# グループ1: 代入とコメントのアライメント
my $x     = 1; # short comment
my $long  = 2; # another comment

# 空行でグループ終了

# グループ2: ファットカンマのアライメント
my %hash = (
    key1   => "value1",
    longer => "value2",
);

# グループ3: コメントのみのアライメント
my $a = 1; # TODO: fix this
my $b = 2; # OK
```

### 7.3 揃える桁 (ALIGNMENT-3)

グループは**一つの桁で揃うか、まったく揃わないかのどちらか**です。中間はありません。

- 揃える桁は、グループ内で最も右にあるアンカーの桁です。
- 桁は**表示幅**で数えます（全角文字は 2 桁）。文字数で数えると、`'あいう'` と
  `'plain'` の混在した表が画面上で揃いません。
- グループ内のアンカーの桁差が `max_alignment_padding`（既定 64）を超える場合、
  そのグループは**一つも揃えません**。長い 1 行がグループ全体を画面の外へ押し出す
  のを防ぐための上限ですが、行ごとにパディングを打ち切ると、同じグループの各行が
  別々の桁に並ぶ——どの尺度でも揃っておらず、しかも再フォーマットで直らない出力に
  なります。

```perl
# 表示幅で揃える
my %map = (
    'あいう'             => 1,
    'あいうえおかきくけ' => 2,
    'plain'              => 3,
);
```

## 8. フォーマットされない構文 (VERBATIM)

(VERBATIM-1) 以下の構文要素は、その内容が一切変更されません。

- **ヒアドキュメント**
- **PODブロック**
- **`__DATA__`セクション**
- **Quote-like演算子** (`q{...}`, `qq(...)`, `s/.../.../` 等) の区切り文字内部

## 9. 今後の拡張 (FUTURE)

- (FUTURE-1) **設定可能なオプション**: インデント幅、タブ/スペース、行の最大長、アライメント戦略など。
- (FUTURE-2) **スタイルプリセット**: PBP, Perl::Tidy互換などのプリセット提供。
- (FUTURE-3) **エディタ統合**: 保存時自動フォーマットや選択範囲のフォーマット。

---

このドキュメントは、実装とテストケースに基づいて作成されましたが、フォーマッタの仕様は進化する可能性があります。不明確な点や矛盾する動作を発見した場合は、GitHub Issues で報告してください。
