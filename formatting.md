# Perl Formatter Specification

このドキュメントは、Camello フォーマッタの動作仕様を定義します。

## 1. 基本方針

### 1.1 不変の原則

- **トリビア以外は変更しない**: フォーマッタはトークン（空白、改行、インデント以外）の内容や順序を変更しません
- **構造の保存**: コードの構造（AST）は変更されません
- **意味の保存**: フォーマット後のコードは、フォーマット前と完全に同じ意味を持ちます

### 1.2 ユーザ意図の尊重

- **改行の尊重**: ユーザが入力した改行は基本的に保持されます
- **構造化フォーマット**: 一部の構文要素（制御構造、サブルーチン定義など）は一貫したフォーマットに正規化されます

## 2. 改行のルール

### 2.1 ユーザ改行の保持

原則として、ユーザが入力した改行は保持されます。

```perl
# ユーザが入力した改行を保持
my $x = 1
    + 2;

my $result = $test
    ? "true_value"
    : "false_value";
```

### 2.2 空行の正規化

連続する空行（2行以上の空行）は1行にまとめられます。

```perl
# 入力（3行の空行）
my $x = 1;



my $y = 2;

# 出力（1行の空行）
my $x = 1;

my $y = 2;
```

### 2.3 文の終端

セミコロン（`;`）の後には改行が挿入されます。

```perl
# 入力
my $x=1;my $y=2;

# 出力
my $x = 1;
my $y = 2;
```

ただし、セミコロンの後にインラインコメントがある場合は、コメントの後に改行します。

```perl
my $x = 1;  # コメント
my $y = 2;
```

### 2.4 制御構造

#### 2.4.1 ブロック形式

開きブレース（`{`）の後と閉じブレース（`}`）の前後には改行が挿入されます。

```perl
# K&Rスタイル
if ($condition) {
    statement;
}

while ($x < 10) {
    process();
}

sub foo {
    my $x = 1;
}
```

#### 2.4.2 単文ブロック（simple block）

以下の条件をすべて満たすブロックは「単文ブロック」として1行に収められることがあります：

- セミコロンが含まれない
- 文が1つ以下
- フェーズブロック（BEGIN, END など）ではない
- 空行がない

```perl
# 単文ブロック
for (; ; ) { }
while ($i < 2) { }

# 複数行ブロック（セミコロンあり）
for my $var (@list) {
    my $x = 1;
    print $x;
}
```

#### 2.4.3 チェーンされたキーワード

`elsif`, `else`, `catch`, `finally`, `when`, `default` は前の閉じブレースと同じ行に配置されます。

```perl
if ($x) {
    foo();
} elsif ($y) {
    bar();
} else {
    baz();
}

try {
    risky();
} catch ($err) {
    handle($err);
} finally {
    cleanup();
}
```

#### 2.4.4 後置条件

後置条件（postfix conditionals）は1行に収められます。

```perl
warn 1 if $err;
print $_ for @values;
say $x while $x > 0;
```

ユーザが改行を入れた場合は、継続インデントが適用されます。

```perl
warn 1
    if $err;
```

### 2.5 リテラルの複数行展開

#### 2.5.1 配列リファレンス

開き括弧の直後（トリビアを除く）に改行がある場合、すべての要素を複数行に展開します。

```perl
# 入力（最初の要素の前に改行あり）
my @items = (
$first, $second, $third
);

# 出力（全要素を複数行に展開）
my @items = (
    $first,
    $second,
    $third
);
```

開き括弧と最初の要素が同じ行にある場合は、ユーザの改行を保持します。

```perl
# ユーザの改行を保持
my %hash = (key1 => 'value1',
    key2 => 'value2');
```

#### 2.5.2 ハッシュリファレンス

配列リファレンスと同じルールが適用されます。

```perl
# 複数行に展開
my %config = (
    key1 => "value1",
    key2 => "value2"
);

# ユーザの改行を保持
my %hash = (key1 => 'value1',
    key2 => 'value2');
```

#### 2.5.3 qw 式

`qw` 式も同様に、開き括弧の直後に改行がある場合は複数行に展開されます。

### 2.6 括弧式の複数行フォーマット

開き括弧の直後に改行がある場合、閉じ括弧まで複数行フォーマットが適用されます。

```perl
# 複数行フォーマット
if (
    $condition1
        && $condition2
) {
    warn "matched";
}

my $output = some_function(
    $arg1,
    $arg2
);
```

### 2.7 ブロック関数呼び出し

`grep`, `map`, `sort` などのブロック関数では、ブロックが複数行の場合は改行されます。

```perl
my @filtered = grep {
    $_->is_valid;
} @items;

my @transformed = map {
    transform($_);
} @data;
```

単文ブロックの条件を満たす場合は1行に収められます。

```perl
# 1行フォーマット（将来的に実装予定）
my @doubled = map { $_ * 2 } @numbers;
```

## 3. インデントのルール

### 3.1 基本インデント

- インデントは4スペース
- ブロック内では1段階（4スペース）インデント
- ネストしたブロックは累積的にインデント

```perl
sub foo {
    if ($x) {
        while ($y) {
            process();
        }
    }
}
```

### 3.2 継続インデント（Continuation Indent）

行が続く場合（式が複数行にわたる場合）、2行目以降に追加で4スペースのインデントが適用されます。

#### 3.2.1 演算子の後

```perl
my $sum = $value1
    + $value2
    + $value3
    * $multiplier;

my $allowed = $user->has_permission()
    && $feature->enabled()
    || $is_admin;
```

#### 3.2.2 カンマの後

```perl
my $result = func($a,
    $b);
```

ただし、複数行の括弧式（開き括弧の直後に改行がある）の場合は、継続インデントではなく通常のインデントが適用されます。

```perl
my $output = some_function(
    $arg1,
    $arg2
);
```

#### 3.2.3 後置キーワード

```perl
warn 1
    if $err;
```

#### 3.2.4 三項演算子

```perl
my $result = $test
    ? "true_value"
    : "false_value";
```

#### 3.2.5 package 文

```perl
package
    My::Module::Name;
```

#### 3.2.6 メソッドチェーン

```perl
my $obj = $factory
    ->create()
    ->initialize()
    ->configure();
```

### 3.3 サブルーチン内での継続インデント

サブルーチン内で継続インデントが発生する場合、ブロックのインデント（4スペース）に継続インデント（4スペース）が加わります。

```perl
sub foo {
    my $result = func($a,
        $b);
}
```

## 4. 空白（Inline Spacing）のルール

### 4.1 演算子周りの空白

#### 4.1.1 二項演算子

二項演算子の前後にスペースを入れます。

```perl
my $x = 1 + 2 * 3;
my $s = "hello" . " " . "world";
$a == $b
$a eq $b
$a && $b
$a || $b
$a // $b
$a cmp $b
$a <=> $b
```

#### 4.1.2 代入演算子

代入演算子（`=` およびその複合形）の前後にスペースを入れます。

```perl
my $x  = 1;
$x    += 2;
$str  .= "suffix";
$flag ||= default_value();
```

複合代入演算子では、演算子と `=` の間にスペースを入れません。

```perl
$x += 1;   # 正しい
$x + = 1;  # 誤り（フォーマッタはこうしない）
```

#### 4.1.3 比較演算子

比較演算子の前後にスペースを入れます。

```perl
$a < $b
$a > $b
$a <= $b
$a >= $b
$a != $b
$a ne $b
```

#### 4.1.4 アロー演算子

アロー演算子（`->`）の前後にはスペースを入れません。

```perl
$obj->method()
$ref->{key}
$ref->[0]
```

#### 4.1.5 範囲演算子

範囲演算子（`..`, `...`）の前後にスペースを入れます。

```perl
1 .. 10
0 ... $#array
```

#### 4.1.6 繰り返し演算子

繰り返し演算子（`x`）の前後にスペースを入れます。

```perl
"a" x 10
q(a) x 10
```

#### 4.1.7 三項演算子

三項演算子（`? :`）では、`?` と `:` の前後にスペースを入れます。

```perl
my $result = $test ? "yes" : "no";
```

ただし、`?` の後にはスペースを入れません（次のトークンの前にスペースが入ります）。

```perl
# 実装では次のようになります
$test ? $yes : $no
#     ^     ^
#   スペース
```

### 4.2 前置・後置演算子

#### 4.2.1 前置演算子

前置演算子とオペランドの間にはスペースを入れません。

```perl
!$flag
~$bits
++$i
--$j
-$x
+$y
```

#### 4.2.2 後置演算子

後置演算子とオペランドの間にはスペースを入れません。

```perl
$i++
$j--
$x->@*
$y->%*
```

#### 4.2.3 ファイルテスト演算子

ファイルテスト演算子の後にはスペースを入れます。

```perl
-f $file
-d $dir
-e $path
```

### 4.3 カンマ

カンマ（`,`）の前にはスペースを入れず、後にはスペースを入れます。

```perl
my @list = (1, 2, 3);
func($a, $b, $c);
```

ただし、末尾のカンマの後にはスペースを入れません（閉じ括弧が続くため）。

```perl
my @list = (1, 2, 3, );
```

### 4.4 ファットカンマ

ファットカンマ（`=>`）は二項演算子として扱われ、前後にスペースを入れます。

```perl
my %hash = (
    key1 => "value1",
    key2 => "value2"
);
```

### 4.5 括弧とブレース

#### 4.5.1 丸括弧

**デフォルト設定（Tight）**：内側にスペースを入れません。

```perl
func($a, $b)
($x + $y) * $z
```

ただし、制御構造の条件式では、開き括弧の前にスペースを入れます。

```perl
if ($condition) { ... }
while ($x < 10) { ... }
for my $i (0 .. 9) { ... }
```

関数呼び出しでは、開き括弧の前にスペースを入れません。

```perl
func($arg)
my_function($x, $y)
```

**設定変更可能**：`DelimiterTightness` 設定で変更可能です。

#### 4.5.2 角括弧

**デフォルト設定（Standard）**：内側に要素が2つ以上ある場合はスペースを入れます。

```perl
[1, 2, 3]    # スペースあり
[$x]         # スペースなし
[]           # スペースなし
```

**設定変更可能**：`DelimiterTightness` 設定で変更可能です。

#### 4.5.3 波括弧

**デフォルト設定（Standard）**：内側に要素が2つ以上ある場合はスペースを入れます。

```perl
{ a => 1, b => 2 }    # スペースあり
{ $x }                # スペースあり（simple blockとして）
{}                    # スペースなし
```

制御構造のブロックでは、内側にスペースを入れません（改行されるため）。

```perl
if ($x) {
    statement;
}
```

**設定変更可能**：`DelimiterTightness` 設定で変更可能です。

#### 4.5.4 開き括弧の前

- 制御構造キーワード（`if`, `while`, `for` など）の後：スペースあり
- 変数の後（デリファレンスや添字）：スペースなし
- 関数呼び出し：スペースなし
- キーワードではない識別子（関数名）：スペースなし

```perl
# スペースあり
if ($x) { }
while ($y) { }
for my $i (@list) { }

# スペースなし
$obj->method($arg)
$hash{key}
$array[0]
func($x, $y)
$var{key}
```

### 4.6 キーワード

#### 4.6.1 宣言キーワード

宣言キーワード（`my`, `our`, `state`, `local`）の後にはスペースを入れます。

```perl
my $x = 1;
our @array = ();
state $counter = 0;
local $/ = undef;
```

#### 4.6.2 制御構造キーワード

制御構造キーワードの後にはスペースを入れます。

```perl
if ($x) { }
unless ($y) { }
while ($z) { }
for my $i (@list) { }
```

#### 4.6.3 return および制御キーワード

`return`, `next`, `last`, `redo` の後は、セミコロンの前を除いてスペースを入れます。

```perl
return $value;
return;        # セミコロンの前はスペースなし
next if $skip;
last LABEL;
```

#### 4.6.4 package, use, require

これらのキーワードの後にはスペースを入れます。

```perl
package Foo::Bar;
use strict;
require Module;
```

### 4.7 シジル

シジル（`$`, `@`, `%`, `&`, `*`）とその後の変数名の間にはスペースを入れません。

```perl
$scalar
@array
%hash
&code
*glob
```

### 4.8 ダブルコロン

ダブルコロン（`::`）の前後にはスペースを入れません。

```perl
Foo::Bar::baz()
my $x = $Foo::Bar::CONSTANT;
```

### 4.9 セミコロン

セミコロンの前にはスペースを入れず、後にはスペースを入れます（ただし、通常は改行されます）。

```perl
my $x = 1; my $y = 2;  # 同じ行の場合
```

### 4.10 変数の後

変数の後に別の変数、識別子、文字列が続く場合はスペースを入れます（ファイルハンドル構文のため）。

```perl
print $fh $output;
print STDERR "error\n";
```

ただし、デリファレンスやアクセス演算子が続く場合はスペースを入れません。

```perl
$var->method()
$var->{key}
$var->[0]
$var->@*
```

## 5. 空行のルール

### 5.1 サブルーチン定義

サブルーチン定義の前後には空行を入れます。

```perl
use strict;

sub foo {
    ...
}

sub bar {
    ...
}
```

ただし、ファイルの先頭やブロックの先頭では前の空行を入れません。

```perl
# ファイルの先頭
sub first {
    ...
}

# サブルーチン内のBEGINブロック
sub foo {
    BEGIN {
        ...
    }
}
```

### 5.2 フェーズブロック

フェーズブロック（`BEGIN`, `END`, `CHECK`, `INIT`, `UNITCHECK`）の前後には空行を入れます。

```perl
use strict;

BEGIN {
    ...
}

CHECK {
    ...
}

END {
    ...
}
```

### 5.3 use/no 文

`use` や `no` 文は、`package` 文の直後を除いて、前後に空行を入れます。

```perl
package Foo;
use strict;    # package の直後は空行なし
use warnings;

sub bar {      # use の後は空行あり
    ...
}
```

### 5.4 通常の文

通常の文（`my` 宣言、式文など）では、ソースコードに空行がある場合のみそれを保持します。自動的に空行を挿入することはありません。

```perl
# ユーザが空行を入れた場合のみ保持
my $x = 1;

my $y = 2;  # ユーザが空行を入れた

my $z = 3;  # ユーザが空行を入れなかった
```

## 6. コメント

### 6.1 行末コメント

行末コメントは、前のトークンとの間にスペース1つを入れて配置します。

```perl
my $x = 1;  # コメント
```

### 6.2 独立行コメント

独立行のコメントは、現在のインデントレベルでインデントされます。

```perl
sub foo {
    # このコメントはサブルーチン内のインデント
    my $x = 1;
}
```

### 6.3 コメントの保持

すべてのコメントは、その内容と相対的な位置を保持します。

```perl
# 入力
my $x=1;# comment

# 出力
my $x = 1;  # comment
```

### 6.4 演算子の後のコメント

演算子の後にコメントがある場合、コメントの後に継続インデントが適用されます。

```perl
my $x = 1
# adjust
    + 2;
```

## 7. アライメント（整列）

### 7.1 代入演算子のアライメント

連続する代入文では、代入演算子（`=` およびその複合形）を揃えます。

```perl
my $x     = 1;
our $y    = 2;
state $z  = 3;
local $w  = 4;
```

複合代入演算子も含めてアライメントされます（デフォルト設定）。

```perl
$x   = 1;
$y  += 2;
$z  .= "str";
```

**設定変更可能**：`align_compound_assignments` オプションで無効化可能です。

### 7.2 ファットカンマのアライメント

ハッシュリテラルやリストでは、ファットカンマ（`=>`）を揃えます。

```perl
my %hash = (
    key1   => "value1",
    key2   => "value2",
    longer => "value3"
);
```

### 7.3 アライメントのスコープ

アライメントは、連続する同種の文の範囲で適用されます。空行や異なる種類の文で区切られます。

```perl
# アライメントグループ1
my $x = 1;
my $y = 2;

# 空行で区切られる

# アライメントグループ2
my $a = 3;
my $b = 4;
```

**設定変更可能**：`alignment_strategies` オプションでアライメント機能を選択的に有効/無効化できます。

## 8. 特殊な構文

### 8.1 ヒアドキュメント

ヒアドキュメントの内容は一切変更されません。

```perl
my $text = <<'EOF';
    このインデントも保持される
    改行も保持される
EOF
```

### 8.2 POD

POD ブロックは一切変更されません。

```perl
=head1 TITLE

このPODは変更されない

=cut
```

### 8.3 __DATA__ セクション

`__DATA__` セクションの内容は一切変更されません。

```perl
__DATA__
このデータは変更されない
    インデントも保持される
```

### 8.4 Quote-like 演算子

Quote-like 演算子（`q`, `qq`, `qw`, `qx`, `m`, `s`, `tr`, `y`, `qr`）の内容は変更されません（デリミタの外側は通常通りフォーマットされます）。

```perl
my $str = q{raw string};
my @words = qw(one two three);
s{pattern}{replacement};
```

### 8.5 ラベル

ラベルは独立行に配置され、後のコロンはそのまま続きます。

```perl
LABEL: while ($condition) {
    next LABEL if $skip;
}
```

ラベルとコロンの間の空白は削除されます。

```perl
# 入力
LABEL : while ($x) { }

# 出力
LABEL: while ($x) { }
```

## 9. 未解決の課題と曖昧な点

### 9.1 単文ブロックの判定

現在の実装では、以下の条件で単文ブロックを判定していますが、より明確な基準が必要かもしれません：

- セミコロンが含まれない
- 文が1つ以下
- フェーズブロックではない
- 空行がない

**議論点**：
- コメントを含むブロックは単文ブロックとして扱うべきか？
- 単一の式文（例: `{ $x + $y }`）は単文ブロックか？

### 9.2 ブロック関数の単文フォーマット

`grep`, `map` などのブロック関数で、単文ブロックを1行にフォーマットする条件が不明確です。

```perl
# 現在の出力
my @doubled = map {
    $_ * 2;
} @numbers;

# 望ましい出力？
my @doubled = map { $_ * 2 } @numbers;
```

**議論点**：
- ブロック関数の単文ブロックはいつ1行にすべきか？
- ユーザが改行を入れた場合は尊重すべきか？

### 9.3 括弧内のスペース

`DelimiterTightness` 設定は柔軟性を提供していますが、以下が不明確です：

- 丸括弧のデフォルトが Tight なのは、関数呼び出しでの慣習に合わせたため
- ただし、数式などでは `( $x + $y )` のようにスペースを入れることもある
- 現在は内側のスペースを入れないが、これが常に望ましいかは議論の余地がある

**議論点**：
- コンテキストに応じて括弧内のスペースを調整すべきか？
- ユーザの設定を尊重する現在の方針で十分か？

### 9.4 空行の自動挿入

現在、サブルーチン定義やフェーズブロックの前後には自動的に空行を挿入しますが、通常の文では挿入しません。

**議論点**：
- 長い式文やコメントの前後にも空行を入れるべきか？
- 論理的なセクション区切りを自動的に検出して空行を入れるべきか？

### 9.5 継続インデントの適用範囲

継続インデントは、演算子の後、カンマの後などで適用されますが、以下が曖昧です：

- 複数行の括弧式では継続インデントを適用しない（通常のインデント）
- しかし、括弧内の途中の行では継続インデントを適用する

```perl
# 通常のインデント
my $output = some_function(
    $arg1,
    $arg2
);

# 継続インデント
my $result = func($a,
    $b);
```

**議論点**：
- この区別は直感的か？
- 一貫した基準を設けるべきか？

### 9.6 後置条件のフォーマット

後置条件を含む文で、ユーザが改行を入れた場合の扱いが曖昧です。

```perl
# 現在の出力
warn 1
    if $err;

# 別の可能性
warn 1 if $err;  # 常に1行にする？
```

**議論点**：
- ユーザの改行を尊重する現在の方針で良いか？
- 短い文は常に1行にすべきか？

### 9.7 メソッドチェーンのフォーマット

メソッドチェーンで改行が入る場合の一貫性が不明確です。

```perl
# 現在の出力（ユーザの改行を保持）
my $obj = $factory
    ->create()
    ->initialize();

# または
my $obj = $factory->create()
    ->initialize();
```

**議論点**：
- 最初のメソッド呼び出しを常に次の行にすべきか？
- チェーンの途中で改行すべき基準は？

### 9.8 アライメントの範囲

アライメントを適用する範囲（スコープ）の定義が曖昧な場合があります。

```perl
# 例: if 文の前後で区切るべきか？
my $x = 1;
if ($condition) {
    my $y = 2;
}
my $z = 3;
```

**議論点**：
- 制御構造で区切るべきか？
- インデントレベルで区切るべきか？

### 9.9 コメントのアライメント

行末コメントのアライメントは実装されていますが、以下が不明確です：

```perl
my $x = 1;    # コメント1
my $y = 2;    # これはコメント2
```

**議論点**：
- コメントを揃えるべきか？
- コメントの内容によって扱いを変えるべきか？

### 9.10 連続する空行の扱い

現在は連続する空行を1行にまとめますが、以下が曖昧です：

- POD ブロックの前後は？
- `__DATA__` セクションの前は？
- ファイルの先頭・末尾は？

**議論点**：
- 特定の境界では複数の空行を許容すべきか？
- ユーザの意図をどこまで尊重すべきか？

## 10. 今後の拡張

### 10.1 設定可能なオプション

以下のオプションを設定可能にすることを検討：

- インデント幅（現在は4スペース固定）
- タブ vs スペース
- 行の最大長（現在は制限なし）
- アライメントの詳細設定

### 10.2 スタイルプリセット

異なるコーディングスタイル（例: PBP, Perl::Tidy 互換など）のプリセットを提供することを検討。

### 10.3 エディタ統合

エディタでの保存時自動フォーマットや、部分的なフォーマット（選択範囲のみ）をサポートすることを検討。

---

このドキュメントは、実装とテストケースに基づいて作成されましたが、フォーマッタの仕様は進化する可能性があります。不明確な点や矛盾する動作を発見した場合は、GitHub Issues で報告してください。
