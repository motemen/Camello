sub compound_alignment {
    my $simple=1;
    my $or ||=2;
    my $sum+=3;
    $flag&&=check();
    $concat .= value();
}

# The `=` is what lines up, whatever the operator's length
sub mixed_widths {
    my $x = 1;
    $y -= 2;
    $longer //= 3;
    $z **= 4;
}
