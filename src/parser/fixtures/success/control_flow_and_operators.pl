return 1, 2;
grep +$_, @list;
shift // 1;
pop // 1;
-f // 0;
my $x = f / 10 / 2;
time // 1;
fork + 1;
wait or die 'oops';
wantarray ? 1 : 0;
say Foo::Bar->method();
warn 1; { warn 2 } warn 3;
my $hash = +{a => 1};
+{}
->{key};
my $hash_kw = +{ package => 1, and => 2 };
(a => [] );
(a => []);
