# Fixtures whose expected output the formatter does not yet produce.
#
# A fixture under `regressions/` is an A→B pair: the `.pl` file is A, and B is
# its `.expected.pl` sibling, or A itself when there is none. Normally
# `format(A) == B` and there is nothing to record. A defect found before it is
# fixed is the exception: the reproduction is worth checking in on the day it is
# found, but it fails by construction until the fix lands.
#
# This file is that record, and it is the only one. A fixture listed here is
# excluded from the invariant sweeps as well: its output is already known to be
# wrong, and asking eight more questions about it only spreads one fact across
# eight lists.
#
# The ledger is monotone. An entry may be removed and never added: a listed
# fixture that starts producing its expected output fails the test just as
# loudly as an unlisted one that stops, so it cannot silence a regression in
# code that works today, and it cannot go stale — the fix that lands is required
# to delete its line.
#
# One path per line, relative to the repository root.

# A comment after the last statement of the file is dropped. Found once
# `feature.pm` started parsing: the file ends with `# ex: set ro ft=perl:`, and
# nothing follows it for the comment to attach to.
src/fmt/fixtures/regressions/trailing_comment_at_end_of_file.pl

# The nine below came out of one sweep of `dev check` over a Perl application of
# some 5500 files: 45 of them violated an invariant, and every violation was one
# of these nine defects or the trailing comment above, which accounted for 27 of
# the 45 on its own.

# `class` / `field` / `method` are not parsed. The `field` lines are read as a
# call with a trailing `:`, and `method f() {` as a call subscripted by a block.
src/fmt/fixtures/regressions/core_class_feature.pl



# `async sub` and `await` are not parsed. `async` is taken for the whole
# declaration, leaving the body without a `sub` to belong to.
src/fmt/fixtures/regressions/async_sub_and_await.pl





# An empty hash literal holding a blank line formats to `{\n}` and then to `{}`,
# so the first pass is not a fixed point.
src/fmt/fixtures/regressions/empty_hash_literal_holding_a_blank_line.pl
