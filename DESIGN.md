# **Perlコードフォーマッタ開発プロジェクト設計書**

## **1\. 概要**

### **1.1. プロジェクトの目的**

このプロジェクトは、Rust製のライブラリ Rowan を利用して、Perlスクリプトを整形するための高機能なコードフォーマッタを開発することを目的とします。RowanのCST（具象構文木）をベースにすることで、元のコードのコメントや空白を保持しつつ、堅牢で柔軟なフォーマットを実現します。

### **1.2. なぜRustとRowanなのか**

* **Rust:** パフォーマンスと安全性が高く、CLIツールの開発に最適です。  
* **Rowan:** エラー耐性が高く、IDEのようなツール開発に適したCSTライブラリです。構文的に正しくないコードでも部分的に解析でき、フォーマッタの実装を容易にします。

### **1.3. 対象範囲**

* **Perlバージョン:** Perl 5.x の一般的な構文を対象とします。  
* **目標:** perltidy の主要な機能のサブセットを実装し、一貫したコーディングスタイルを適用できるようにします。

## **2\. 主要な機能**

### **2.1. フォーマットルール**

以下のような基本的なフォーマットルールを実装します。

* **インデント:**  
  * インデントスタイルは **スペース4つ** を基本とします。  
  * {} ブロック、サブルーチン定義、制御構造（if, for, whileなど）の本体をインデントします。  
* **スペース:**  
  * 二項演算子（+, \-, \*, /, \=, .など）の前後にはスペースを1つ入れます。  
  * カンマ,やセミコロン;の後にはスペースを1つ入れます。  
  * サブルーチン呼び出しの関数名と開き括弧(の間にはスペースを入れません。  
* **改行:**  
  * ブロックの開きブレース{は、キーワード（sub, ifなど）と **同じ行** に配置します（K\&Rスタイル）。  
  * 文の終わり（;）で改行します。  
* **その他:**  
  * 連続する空行は最大1行にまとめます。

### **2.2. CLI（コマンドラインインターフェース）**

clapクレートを利用して、biomeのようにサブコマンドを持つCLIを実装します。

**コマンド体系:**

camello \<SUBCOMMAND\>

**サブコマンド:**

* format \<PATH\>: 指定されたファイルをフォーマットし、結果を標準出力に出力します。  
  * **\<PATH\>**: フォーマット対象のファイルまたはディレクトリのパス。

## **3\. アーキテクチャ**

### **3.1. 概要図**

Perlソースコードから整形済みコードが出力されるまでの処理フローは以下の通りです。

Perlソースコード \-\> \[Lexer\] \-\> トークン列 \-\> \[Parser\] \-\> CST (Rowan) \-\> \[Formatter\] \-\> 整形済みコード

### **3.2. コンポーネント詳細**

#### **3.2.1. パーサー (Parser)**

* **字句解析 (Lexing):** まず、Perlコードを意味のある最小単位（トークン）に分割します。トークンには、キーワード (sub, if)、識別子 ($variable)、演算子 (+)、シンボル ({, })、リテラル ("string") などが含まれます。  
* **構文解析 (Parsing):** トークン列からRowanのCSTを構築します。このCSTは、元のソースコードのすべての情報（コメント、空白を含む）を保持するロスレスなツリーです。  
  * Perlの構文要素（文、式、ブロックなど）に対応する SyntaxKind という enum を定義します。  
  * 再帰下降法を用いてパーサーを実装します。構文エラーが発生してもパニックせず、エラー箇所をERRORノードとしてツリーに含め、解析を続行します。

#### **3.2.2. フォーマッタ (Formatter)**

* **CSTの走査 (Traversal):** 生成されたCSTを深さ優先探索で走査します。  
* **フォーマットロジック:**  
  * 各ノード (SyntaxNode) とトークン (SyntaxToken) を訪れる際に、定義されたフォーマットルールを適用します。  
  * インデントレベルを管理するカウンタを保持します。ブロックに入る ({) ときにインデントを増やし、出るとき (}) に減らします。  
  * 先行するトークンと現在のトークンの種類に応じて、スペースや改行を挿入・削除します。  
  * 整形後のコードを文字列バッファに書き出していきます。

## **4\. 開発ステップ**

以下のステップで段階的に開発を進めてください。

1. **Step 1: プロジェクトセットアップ**  
   * cargo new perl\_formatter でプロジェクトを作成します。  
   * Cargo.tomlにrowan, smol\_str の依存関係を追加します。  
2. **Step 2: SyntaxKindの定義**  
   * Perlの構文要素に対応するSyntaxKindのenumを定義します。最初はサブルーチン定義、変数宣言、簡単な演算などの基本的な要素から始めます。  
   * logosクレートを利用して、正規表現に基づいた字句解析器（Lexer）を実装します。  
3. **Step 3: パーサーの実装**  
   * 単純な構文（例: my $var \= 1;）をパースできる基本的なパーサーを実装します。  
   * CSTが正しく構築されることを確認するテストを作成します。  
4. **Step 4: フォーマッタの骨格実装**  
   * CSTを受け取り、それを再帰的に走査して元のコードとほぼ同じ文字列を再構築する基本的なフォーマッタを実装します。  
   * インデントルールを適用するロジック（{でインデントレベルを上げ、}で下げる）を追加します。  
5. **Step 5: フォーマットルールの拡充**  
   * ステップ2で定義したフォーマットルール（スペース、改行など）を順次実装していきます。  
   * 各ルールに対応するユニットテストやスナップショットテストを追加します。  
6. **Step 6: CLIの実装**  
   * clapを使って、formatサブコマンドを実装します。  
   * ファイルI/O処理を追加し、指定されたファイルを読み込んでフォーマットし、結果を**標準出力に**書き出す機能を実装します。  
7. **Step 7: テストとリファクタリング**  
   * より複雑なPerlコードに対するテストケースを追加します。  
   * コード全体のリファクタリングを行い、可読性と保守性を向上させます。

## **5\. データ構造**

### **SyntaxKind.rs**

Perlの構文要素を表現するための enum を定義します。\#\[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)\] を付与してください。

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]#[allow(non_camel_case_types)]#[repr(u16)]pub enum SyntaxKind {    // ===== トークン =====    WHITESPACE, // 空白    COMMENT,    // コメント (#...)        // 変数    IDENT,      // 識別子 (e.g., mysub)    SCALAR_VAR, // スカラー変数 (e.g., $var)    ARRAY_VAR,  // 配列変数 (e.g., @list)    HASH_VAR,   // ハッシュ変数 (e.g., %hash)        // リテラル    NUMBER,     // 数値 (e.g., 123, 4.56)    STRING,     // 文字列 (e.g., "hello")    // キーワード    SUB_KW,     // 'sub'    MY_KW,      // 'my'    IF_KW,      // 'if'    ELSE_KW,    // 'else'        // シンボル    L_BRACE,    // {    R_BRACE,    // }    L_PAREN,    // (    R_PAREN,    // )    SEMICOLON,  // ;    COMMA,      // ,        // 演算子    EQ,         // =    PLUS,       // +    MINUS,      // -        // ===== 複合ノード =====    ROOT,           // ファイルのルート    SUB_DEF,        // サブルーチン定義 (sub name { ... })    BLOCK_STMT,     // ブロック文 ({ ... })    VAR_DECL,       // 変数宣言 (my $var = 1;)    BINARY_EXPR,    // 二項演算式 (e.g., $a + $b)        // ===== その他 =====    ERROR,      // 解析エラー}
```

### **パーサーの出力**

パーサーは、rowan::GreenNode（CSTの本体）と解析中に発生したエラーのリストを返す構造にします。これをrowan::SyntaxNodeでラップして利用します。

## **6\. 考慮事項**

* **Perlの複雑な構文:** Perlには文脈依存の構文や後置の制御構造 (... if ...;) など、解析が難しい要素が多くあります。最初はコアとなる基本的な構文のみをサポートし、徐々に対応範囲を広げていくアプローチを取ります。  
* **テスト戦略:**  
  * **ユニットテスト:** 各パーサー関数やフォーマットロジックの単体テストを記述します。  
  * **フォーマットテスト:** 特定のPerlコードを入力とし、フォーマット後の文字列全体が期待される出力と完全に一致するかを検証するテストを主体とします。これにより、テストの意図が明確になり、仕様の確認が容易になります。スナップショットテスト（instaクレートなど）の活用を推奨します。  
* **設定ファイル:** 将来的には.perltidyrcのような設定ファイルを導入し、インデント幅や改行スタイルなどをユーザーがカスタマイズできる機能を検討します。

## **7\. パーサー設計の改善提案（Geminiより）**

Perlの構文が持つ複雑さや文脈依存性に対応し、将来的な拡張性を高めるため、特にパーサーの設計に関して以下の改善を提案します。

### **7.1. 式のパースにPratt Parsing（トップダウン演算子順位パーシング）を導入する**

*   **提案内容:** 演算子の優先順位と結合規則を効率的かつ正確に扱うため、式のパース部分にPratt Parsingを導入することを推奨します。
*   **理由:** Perlの多様な演算子（二項、単項、三項）を再帰下降法だけで扱うとコードが複雑になりがちです。Pratt Parsingは、これらの演算子を統一的かつシンプルに表現でき、パーサーの見通しを良くします。

### **7.2. Sigil（`# **Perlコードフォーマッタ開発プロジェクト設計書**

## **1\. 概要**

### **1.1. プロジェクトの目的**

このプロジェクトは、Rust製のライブラリ Rowan を利用して、Perlスクリプトを整形するための高機能なコードフォーマッタを開発することを目的とします。RowanのCST（具象構文木）をベースにすることで、元のコードのコメントや空白を保持しつつ、堅牢で柔軟なフォーマットを実現します。

### **1.2. なぜRustとRowanなのか**

* **Rust:** パフォーマンスと安全性が高く、CLIツールの開発に最適です。  
* **Rowan:** エラー耐性が高く、IDEのようなツール開発に適したCSTライブラリです。構文的に正しくないコードでも部分的に解析でき、フォーマッタの実装を容易にします。

### **1.3. 対象範囲**

* **Perlバージョン:** Perl 5.x の一般的な構文を対象とします。  
* **目標:** perltidy の主要な機能のサブセットを実装し、一貫したコーディングスタイルを適用できるようにします。

## **2\. 主要な機能**

### **2.1. フォーマットルール**

以下のような基本的なフォーマットルールを実装します。

* **インデント:**  
  * インデントスタイルは **スペース4つ** を基本とします。  
  * {} ブロック、サブルーチン定義、制御構造（if, for, whileなど）の本体をインデントします。  
* **スペース:**  
  * 二項演算子（+, \-, \*, /, \=, .など）の前後にはスペースを1つ入れます。  
  * カンマ,やセミコロン;の後にはスペースを1つ入れます。  
  * サブルーチン呼び出しの関数名と開き括弧(の間にはスペースを入れません。  
* **改行:**  
  * ブロックの開きブレース{は、キーワード（sub, ifなど）と **同じ行** に配置します（K\&Rスタイル）。  
  * 文の終わり（;）で改行します。  
* **その他:**  
  * 連続する空行は最大1行にまとめます。

### **2.2. CLI（コマンドラインインターフェース）**

clapクレートを利用して、biomeのようにサブコマンドを持つCLIを実装します。

**コマンド体系:**

camello \<SUBCOMMAND\>

**サブコマンド:**

* format \<PATH\>: 指定されたファイルをフォーマットし、結果を標準出力に出力します。  
  * **\<PATH\>**: フォーマット対象のファイルまたはディレクトリのパス。

## **3\. アーキテクチャ**

### **3.1. 概要図**

Perlソースコードから整形済みコードが出力されるまでの処理フローは以下の通りです。

Perlソースコード \-\> \[Lexer\] \-\> トークン列 \-\> \[Parser\] \-\> CST (Rowan) \-\> \[Formatter\] \-\> 整形済みコード

### **3.2. コンポーネント詳細**

#### **3.2.1. パーサー (Parser)**

* **字句解析 (Lexing):** まず、Perlコードを意味のある最小単位（トークン）に分割します。トークンには、キーワード (sub, if)、識別子 ($variable)、演算子 (+)、シンボル ({, })、リテラル ("string") などが含まれます。  
* **構文解析 (Parsing):** トークン列からRowanのCSTを構築します。このCSTは、元のソースコードのすべての情報（コメント、空白を含む）を保持するロスレスなツリーです。  
  * Perlの構文要素（文、式、ブロックなど）に対応する SyntaxKind という enum を定義します。  
  * 再帰下降法を用いてパーサーを実装します。構文エラーが発生してもパニックせず、エラー箇所をERRORノードとしてツリーに含め、解析を続行します。

#### **3.2.2. フォーマッタ (Formatter)**

* **CSTの走査 (Traversal):** 生成されたCSTを深さ優先探索で走査します。  
* **フォーマットロジック:**  
  * 各ノード (SyntaxNode) とトークン (SyntaxToken) を訪れる際に、定義されたフォーマットルールを適用します。  
  * インデントレベルを管理するカウンタを保持します。ブロックに入る ({) ときにインデントを増やし、出るとき (}) に減らします。  
  * 先行するトークンと現在のトークンの種類に応じて、スペースや改行を挿入・削除します。  
  * 整形後のコードを文字列バッファに書き出していきます。

## **4\. 開発ステップ**

以下のステップで段階的に開発を進めてください。

1. **Step 1: プロジェクトセットアップ**  
   * cargo new perl\_formatter でプロジェクトを作成します。  
   * Cargo.tomlにrowan, smol\_str の依存関係を追加します。  
2. **Step 2: SyntaxKindの定義**  
   * Perlの構文要素に対応するSyntaxKindのenumを定義します。最初はサブルーチン定義、変数宣言、簡単な演算などの基本的な要素から始めます。  
   * logosクレートを利用して、正規表現に基づいた字句解析器（Lexer）を実装します。  
3. **Step 3: パーサーの実装**  
   * 単純な構文（例: my $var \= 1;）をパースできる基本的なパーサーを実装します。  
   * CSTが正しく構築されることを確認するテストを作成します。  
4. **Step 4: フォーマッタの骨格実装**  
   * CSTを受け取り、それを再帰的に走査して元のコードとほぼ同じ文字列を再構築する基本的なフォーマッタを実装します。  
   * インデントルールを適用するロジック（{でインデントレベルを上げ、}で下げる）を追加します。  
5. **Step 5: フォーマットルールの拡充**  
   * ステップ2で定義したフォーマットルール（スペース、改行など）を順次実装していきます。  
   * 各ルールに対応するユニットテストやスナップショットテストを追加します。  
6. **Step 6: CLIの実装**  
   * clapを使って、formatサブコマンドを実装します。  
   * ファイルI/O処理を追加し、指定されたファイルを読み込んでフォーマットし、結果を**標準出力に**書き出す機能を実装します。  
7. **Step 7: テストとリファクタリング**  
   * より複雑なPerlコードに対するテストケースを追加します。  
   * コード全体のリファクタリングを行い、可読性と保守性を向上させます。

## **5\. データ構造**

### **SyntaxKind.rs**

Perlの構文要素を表現するための enum を定義します。\#\[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)\] を付与してください。

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]#[allow(non_camel_case_types)]#[repr(u16)]pub enum SyntaxKind {    // ===== トークン =====    WHITESPACE, // 空白    COMMENT,    // コメント (#...)        // 変数    IDENT,      // 識別子 (e.g., mysub)    SCALAR_VAR, // スカラー変数 (e.g., $var)    ARRAY_VAR,  // 配列変数 (e.g., @list)    HASH_VAR,   // ハッシュ変数 (e.g., %hash)        // リテラル    NUMBER,     // 数値 (e.g., 123, 4.56)    STRING,     // 文字列 (e.g., "hello")    // キーワード    SUB_KW,     // 'sub'    MY_KW,      // 'my'    IF_KW,      // 'if'    ELSE_KW,    // 'else'        // シンボル    L_BRACE,    // {    R_BRACE,    // }    L_PAREN,    // (    R_PAREN,    // )    SEMICOLON,  // ;    COMMA,      // ,        // 演算子    EQ,         // =    PLUS,       // +    MINUS,      // -        // ===== 複合ノード =====    ROOT,           // ファイルのルート    SUB_DEF,        // サブルーチン定義 (sub name { ... })    BLOCK_STMT,     // ブロック文 ({ ... })    VAR_DECL,       // 変数宣言 (my $var = 1;)    BINARY_EXPR,    // 二項演算式 (e.g., $a + $b)        // ===== その他 =====    ERROR,      // 解析エラー}
```

### **パーサーの出力**

パーサーは、rowan::GreenNode（CSTの本体）と解析中に発生したエラーのリストを返す構造にします。これをrowan::SyntaxNodeでラップして利用します。

## **6\. 考慮事項**

* **Perlの複雑な構文:** Perlには文脈依存の構文や後置の制御構造 (... if ...;) など、解析が難しい要素が多くあります。最初はコアとなる基本的な構文のみをサポートし、徐々に対応範囲を広げていくアプローチを取ります。  
* **テスト戦略:**  
  * **ユニットテスト:** 各パーサー関数やフォーマットロジックの単体テストを記述します。  
  * **フォーマットテスト:** 特定のPerlコードを入力とし、フォーマット後の文字列全体が期待される出力と完全に一致するかを検証するテストを主体とします。これにより、テストの意図が明確になり、仕様の確認が容易になります。スナップショットテスト（instaクレートなど）の活用を推奨します。  
, `@`, `%`）のトークナイズ方法を再検討する**

*   **提案内容:** Sigil（例: `DOLLAR`, `AT`, `PERCENT`）と識別子（`IDENT`）を、それぞれ独立したトークンとして切り出すことを検討してください。
*   **理由:** `$foo->{bar}[0]` のような複雑な変数デリファレンスをパースする際、Sigilと識別子を分離しておくことで、パーサーがより柔軟な構造で複合ノードを組み立てられるようになり、将来的な拡張性が向上します。

### **7.3. `SyntaxKind`の粒度と命名をより汎用的にする**

*   **提案内容:** `SyntaxKind`の命名と粒度を、より抽象的で汎用的なものにすることを推奨します。
*   **具体例:**
    *   `VAR_DECL` → `DECLARATION_STMT` に変更し、`my`, `our`, `state` などのキーワードをその子ノードとして持つ構造にする。
    *   `BINARY_EXPR` → `INFIX_EXPR`（中置式）とし、`PREFIX_EXPR`（前置式、例: `!$foo`）や `POSTFIX_EXPR`（後置式、例: `$i++`）といった分類を導入する。
    *   Perl特有の後置修飾子（`... if ...`）のために、`IF_MODIFIER` のようなKindを早期に検討する。

### **7.4. Lexerに状態（State）管理を導入する**

*   **提案内容:** Lexerが現在の文脈に応じた状態を持つ「ステートフルなLexer」を設計に組み込むことを推奨します。
*   **理由:** Perlには `qw/.../`、`q{...}`、`s{...}{...}`、ヒアドキュメントなど、開始デリミタによって終了条件や内部の解釈ルールが動的に変わる構文が多数存在します。Lexerが状態を管理することで、これらの複雑なトークンを正確に切り出すことができます。
